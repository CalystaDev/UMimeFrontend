import { Mime } from "./mimes.model";

export interface Hosts {
    decription: string;
    displayName: string;
    profilePictureURL: string;
    tags: string[];
    wpm: number;
    uses: number;
}